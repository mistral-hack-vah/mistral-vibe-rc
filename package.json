{
  "name": "thread",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter mobile web",
    "dev:mobile": "pnpm --filter mobile start",
    "dev:server": "pnpm --filter server dev",
    "build:ios": "pnpm --filter mobile ios",
    "build:android": "pnpm --filter mobile android",
    "build:server": "pnpm --filter server build",
    "dev": "pnpm run dev:web & pnpm run dev:server"
  },
  "pnpm": {
    "overrides": {
      "lightningcss": "1.30.1"
    }
  }
}
